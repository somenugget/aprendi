<p style="color: green"><%= notice %></p>

<%= render @study_set %>

<div>
  <%= link_to "Edit this study set", edit_study_set_path(@study_set) %> |
  <%= link_to "Back to study sets", study_sets_path %>

  <%= button_to "Destroy this study set", @study_set, method: :delete %>
</div>
