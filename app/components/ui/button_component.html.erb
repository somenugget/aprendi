<%= content_tag as, **{ **options, class: html_class } do %>
  <% if icon.present? %>
    <%= heroicon icon, options: { class: icon_class } %>
  <% end %>

  <%= content? ? content : text %>
<% end %>
